import { ParseToArrayPipe } from './parse-to-array.pipe';

describe('ParseToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
